spring:
  application:
    name: flash-ledger

  datasource:
#    url: jdbc:mysql://localhost:3307/flashledger
    url: jdbc:mysql://localhost:3306/flashledger
    username: root
    password: root

logging:
  level:
    root: INFO

flash-ledger:
  lock:
    strategy: fail-fast
    wait-time-seconds: 3 # will be used only when strategy = 'bounded-wait'