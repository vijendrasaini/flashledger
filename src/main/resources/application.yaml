spring:
  application:
    name: flash ledger

  datasource:
#    url: jdbc:mysql://localhost:3307/flashledger
    url: jdbc:mysql://localhost:3306/flashledger
    username: root
    password: root

logging:
  level:
    root: INFO